using UnityEngine;

public class BreakableSpot : MonoBehaviour
{
    // we just need to hold a reference to the spawned instance to know if it's already spawned
    public PickableSpawner pickableSpawner;
}
