using System;
using UnityEngine;

[AttributeUsage(AttributeTargets.Field)]
public class ReadOnlyAttribute : PropertyAttribute
{
    // This class is intentionally left blank.
    // The presence of the attribute itself is enough.
}
